Rules = [
    IsComplete "order_id",
    IsComplete "customer_id",
    IsComplete "status",
    IsComplete "order_total",
	IsComplete "order_date",
    IsUnique "order_id",
    ColumnValues "order_total" >= 0
]
